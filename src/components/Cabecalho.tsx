import Menu from "./Menu";

export default function Caecalho(){

    return(
        <header className="flex w-[100vw] justify-around h-[8vh] bg-sky-500">
            <Menu/>
        </header>

    )
}